name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  macos:
    name: macOS (Xcode)
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: swift build

      - name: Test
        run: swift test

  linux:
    name: Linux (Ubuntu)
    runs-on: ubuntu-latest
    container:
      image: swift:6.2
    steps:
      - uses: actions/checkout@v4

      - name: Install SQLite
        run: apt-get update && apt-get install -y libsqlite3-dev

      - name: Build
        run: swift build

      - name: Test
        run: swift test
